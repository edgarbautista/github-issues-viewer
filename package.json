{
  "name": "github-issues-viewer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently \"cd backend && yarn run dev\" \"cd frontend && yarn start\" ",
    "deploy": "concurrently \"cd backend && yarn run start\" \"cd frontend && node_modules/serve/bin/serve.js -s build\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Edgar Bautista",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.5.1"
  },
  "dependencies": {
    "serve": "^13.0.2"
  }
}
